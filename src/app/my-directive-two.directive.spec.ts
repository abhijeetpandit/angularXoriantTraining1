import { MyDirectiveTwoDirective } from './my-directive-two.directive';

describe('MyDirectiveTwoDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDirectiveTwoDirective();
    expect(directive).toBeTruthy();
  });
});
